# Быстрый старт: Новые схемы освещения

## ✅ Что сделано

Добавлены 4 профессиональные схемы освещения:

1. **📸 Студия** - трехточечное освещение (рекомендуется)
2. **🏛️ Архитектура** - реалистичное солнечное освещение
3. **🎪 Выставка** - яркое равномерное освещение
4. **🔧 Базовое** - оригинальное простое освещение

## 🎮 Как переключать

### Способ 1: Через UI (рекомендуется)

1. Откройте приложение
2. Нажмите кнопку **⚙️ Настройки** внизу экрана
3. В самом верху панели найдите секцию **"Схема освещения"**
4. Выберите один из 4 вариантов

### Способ 2: Через консоль браузера

1. Откройте консоль (F12)
2. Выполните команды:

```javascript
// Студийное освещение (рекомендуется)
useAppStore.getState().setLightingPreset('studio')

// Архитектурное освещение
useAppStore.getState().setLightingPreset('architectural')

// Выставочное освещение
useAppStore.getState().setLightingPreset('exhibition')

// Базовое освещение
useAppStore.getState().setLightingPreset('custom')
```

## 🌟 Рекомендации

### Для вашего проекта (МФФ Навигация)

**Лучший выбор: 📸 Студийное освещение**

Почему:
- ✅ Профессиональный вид
- ✅ Хорошая детализация модели
- ✅ Мягкие естественные тени
- ✅ Баланс между реализмом и производительностью
- ✅ Поддержка времени суток

### Альтернативы:

**🏛️ Архитектурное** - если нужен максимальный реализм
- Динамическое солнце
- Реалистичные тени
- Ночное освещение (фонари)
- Требует больше GPU

**🎪 Выставочное** - если нужна максимальная яркость
- Без темных областей
- Минимальные тени
- Яркий "чистый" вид
- Стиль Netlify 5M Devs

## 🎨 Дополнительные настройки

### Для архитектурного освещения

Измените время суток для разных эффектов:

```javascript
// Через консоль
useAppStore.getState().setTimeOfDay(6)   // Рассвет
useAppStore.getState().setTimeOfDay(12)  // Полдень
useAppStore.getState().setTimeOfDay(18)  // Закат
useAppStore.getState().setTimeOfDay(22)  // Ночь
```

### Вращение света

Все схемы освещения привязаны к вращению HDRI:

```javascript
// Через консоль
useAppStore.getState().setHdriRotation(0)    // Север
useAppStore.getState().setHdriRotation(90)   // Восток
useAppStore.getState().setHdriRotation(180)  // Юг
useAppStore.getState().setHdriRotation(270)  // Запад
```

Или используйте слайдер в UI (если есть).

## 📊 Сравнение производительности

| Схема | FPS Impact | Shadow Quality | Источников света |
|-------|-----------|----------------|------------------|
| 📸 Студия | Средний | Высокое | 6-8 |
| 🏛️ Архитектура | Высокий | Очень высокое | 4-8 |
| 🎪 Выставка | Средний | Среднее | 11 |
| 🔧 Базовое | Минимальный | Среднее | 2 |

## 🐛 Если что-то не работает

### Освещение не меняется

1. Проверьте консоль на ошибки (F12)
2. Убедитесь, что приложение перезагрузилось
3. Попробуйте переключить через консоль:
   ```javascript
   useAppStore.getState().setLightingPreset('studio')
   ```

### Слишком темно/светло

Настройте экспозицию tone mapping:

```javascript
// Через консоль
useAppStore.getState().setToneMappingExposure(1.0)  // Темнее
useAppStore.getState().setToneMappingExposure(1.5)  // Светлее
useAppStore.getState().setToneMappingExposure(2.0)  // Еще светлее
```

### Низкий FPS

1. Переключитесь на **Базовое** освещение
2. Или уменьшите качество графики в настройках
3. Или уменьшите разрешение теней (требует изменения кода)

## 📚 Полная документация

Подробное описание каждой схемы освещения см. в файле:
**LIGHTING_PRESETS_GUIDE.md**

## 🔄 Возврат к оригинальному освещению

Если хотите вернуться к оригинальному освещению:

```javascript
useAppStore.getState().setLightingPreset('custom')
```

Или выберите **🔧 Базовое** в UI.

## 💡 Советы

1. **Начните со Студийного** - это лучший баланс
2. **Попробуйте Архитектурное ночью** - красивые фонари
3. **Используйте Выставочное для скриншотов** - максимальная яркость
4. **Комбинируйте с HDRI** - меняйте окружение для разных эффектов

## 🎯 Следующие шаги

1. Попробуйте все 4 схемы
2. Выберите лучшую для вашего проекта
3. Настройте время суток (для архитектурного)
4. Настройте вращение HDRI
5. Настройте tone mapping exposure

---

*Создано: 2026-02-13*
*Версия: 1.0*
