# Руководство по пресетам освещения

## Обзор

В проекте реализованы 4 варианта освещения, каждый оптимизирован для разных сценариев использования.

## Переключение между пресетами

### В коде
```typescript
import { useAppStore } from './store/appStore'

const setLightingPreset = useAppStore(state => state.setLightingPreset)

// Переключить на студийное освещение
setLightingPreset('studio')

// Переключить на архитектурное освещение
setLightingPreset('architectural')

// Переключить на выставочное освещение
setLightingPreset('exhibition')

// Вернуться к оригинальному освещению
setLightingPreset('custom')
```

### Через консоль браузера
```javascript
// Открыть консоль (F12) и выполнить:
window.setLighting = (preset) => {
  const store = window.__ZUSTAND_STORE__
  if (store) store.setState({ lightingPreset: preset })
}

// Затем использовать:
setLighting('studio')
setLighting('architectural')
setLighting('exhibition')
setLighting('custom')
```

---

## Вариант 1: Studio Lighting (Студийное освещение)

### 📸 Описание
Профессиональная трехточечная схема освещения, используемая в фотостудиях и 3D рендеринге.

### ✨ Характеристики
- **Key Light** - основной направленный свет (45° сверху и сбоку)
- **Fill Light** - заполняющий свет (противоположная сторона, без теней)
- **Rim Light** - контровой свет (сзади, для выделения контуров)
- **Ambient Light** - базовое окружающее освещение
- **Hemisphere Light** - небо и земля
- **Point Lights** - дополнительные акценты

### 🎯 Когда использовать
- Для презентаций и демонстраций
- Когда нужна хорошая детализация объектов
- Для профессионального вида
- Баланс между реализмом и визуальным качеством

### 🔧 Технические параметры

#### Key Light (Основной свет)
```typescript
position: [keyLightX, 40, keyLightZ]  // Динамическая позиция
intensity: 2.5 * (0.5 + dayFactor * 0.5)
color: '#ffffff'
castShadow: true
shadow-mapSize: [2048, 2048]
shadow-radius: 3  // Мягкие тени
```

#### Fill Light (Заполняющий)
```typescript
position: [fillLightX, 25, fillLightZ]
intensity: 1.2
color: '#d4e4ff'  // Холодный оттенок
castShadow: false
```

#### Rim Light (Контровой)
```typescript
position: [rimLightX, 50, rimLightZ]
intensity: 1.5
color: '#ffeedd'  // Теплый оттенок
castShadow: false
```

#### Ambient Light
```typescript
intensity: 0.4 + nightFactor * 0.2
color: '#f0f0f5'
```

#### Hemisphere Light
```typescript
skyColor: '#b8d4ff'
groundColor: '#8a8a8a'
intensity: 0.6 + nightFactor * 0.3
```

### 🌓 Поддержка времени суток
- **День**: Полная интенсивность всех источников
- **Ночь**: Добавляются точечные источники снизу (теплый свет)
- Плавный переход между режимами

### 📊 Производительность
- **Shadow maps**: 2048x2048 (средняя нагрузка)
- **Количество источников**: 6-8 (зависит от времени суток)
- **FPS impact**: Средний

### 🎨 Визуальный результат
- Мягкие, естественные тени
- Хорошая детализация
- Объемность объектов
- Профессиональный вид

### 💡 Советы по настройке
```typescript
// Увеличить контраст теней
keyLight.intensity = 3.0
fillLight.intensity = 0.8

// Сделать тени мягче
keyLight.shadow.radius = 5

// Усилить контровой свет
rimLight.intensity = 2.0
```

---

## Вариант 2: Architectural Lighting (Архитектурное освещение)

### 🏛️ Описание
Реалистичное освещение, имитирующее естественное солнечное освещение для архитектурной визуализации.

### ✨ Характеристики
- **Sun Light** - динамическое солнце (меняется по времени суток)
- **Sky Light** - рассеянный свет от неба
- **Bounce Light** - отраженный свет от земли
- **Hemisphere Light** - небо и земля с динамическим цветом
- **Night Lights** - искусственные источники (уличные фонари)

### 🎯 Когда использовать
- Для реалистичной визуализации
- Архитектурные презентации
- Когда важна точность освещения
- Для демонстрации времени суток

### 🔧 Технические параметры

#### Sun Light (Солнце)
```typescript
// Позиция рассчитывается по времени суток
hourAngle: ((timeOfDay - 6) / 12) * Math.PI
sunElevation: Math.sin(hourAngle) * 60
position: [sunX, sunY, sunZ]  // Динамическая

// Интенсивность по времени суток
День (8-18): 3.5
Рассвет/Закат (6-8, 18-20): 2.0
Ночь (20-6): 0.3

// Цвет по времени суток
День: '#fffaf0'  // Теплый белый
Рассвет/Закат: '#ffb366'  // Оранжевый
Ночь: '#4a5a7a'  // Холодный синий

castShadow: true
shadow-mapSize: [4096, 4096]  // Высокое разрешение
shadow-radius: 1.5  // Четкие тени
```

#### Sky Light (Небо)
```typescript
position: [0, 50, 0]
intensity: 0.5
color: динамический (зависит от времени)
castShadow: false
```

#### Bounce Light (Отражение от земли)
```typescript
position: [0, -20, 0]
intensity: 0.3
color: '#b8a890'  // Теплый земляной
castShadow: false
```

#### Hemisphere Light
```typescript
// Цвет неба меняется по времени
День: '#87ceeb'  // Голубое небо
Рассвет/Закат: '#ff9a76'  // Оранжевое небо
Ночь: '#1a1a2e'  // Темное небо

groundColor: '#6a6a6a'
intensity: 0.8
```

#### Night Lights (Ночные фонари)
```typescript
// Активируются при timeOfDay < 6 или > 19
4 точечных источника по углам:
position: [±30, 8, ±30]
intensity: 3
color: '#ffd4a3'  // Теплый желтый
distance: 60
decay: 2
castShadow: true (2 из 4)
shadow-mapSize: [512, 512]
```

### 🌓 Динамика времени суток

#### Рассвет (6:00 - 8:00)
- Солнце низко над горизонтом
- Оранжевый теплый свет
- Длинные тени
- Интенсивность: 2.0

#### День (8:00 - 18:00)
- Солнце высоко
- Белый свет
- Короткие тени
- Интенсивность: 3.5

#### Закат (18:00 - 20:00)
- Солнце низко
- Оранжевый теплый свет
- Длинные тени
- Интенсивность: 2.0

#### Ночь (20:00 - 6:00)
- Минимальное солнечное освещение
- Холодный синий оттенок
- Искусственные источники (фонари)
- Интенсивность: 0.3

### 📊 Производительность
- **Shadow maps**: 4096x4096 (высокая нагрузка)
- **Количество источников**: 4-8 (зависит от времени)
- **FPS impact**: Высокий (из-за высокого разрешения теней)

### 🎨 Визуальный результат
- Максимальный реализм
- Четкие, естественные тени
- Динамическое изменение атмосферы
- Реалистичные цвета

### 💡 Советы по настройке
```typescript
// Для лучшей производительности
sunLight.shadow.mapSize = [2048, 2048]

// Для более мягких теней
sunLight.shadow.radius = 3

// Изменить время суток
useAppStore.getState().setTimeOfDay(14) // 14:00 (день)
useAppStore.getState().setTimeOfDay(6)  // 6:00 (рассвет)
useAppStore.getState().setTimeOfDay(22) // 22:00 (ночь)
```

---

## Вариант 3: Exhibition Lighting (Выставочное освещение)

### 🎪 Описание
Яркое, чистое освещение без резких теней. В стиле Netlify 5M Devs - playful и vibrant.

### ✨ Характеристики
- **Высокое ambient освещение** - общая яркость
- **Множество источников** - равномерное освещение
- **Очень мягкие тени** - минимальный контраст
- **Точечные акценты** - по углам и сверху
- **Нижний свет** - устранение темных областей

### 🎯 Когда использовать
- Для ярких, веселых презентаций
- Выставки и демонстрации
- Когда нужна максимальная видимость
- Стиль "cartoon" или "playful"

### 🔧 Технические параметры

#### Ambient Light
```typescript
intensity: 1.2  // Очень высокая
color: '#ffffff'
```

#### Hemisphere Light
```typescript
skyColor: '#ffffff'
groundColor: '#e8e8e8'  // Светлая земля
intensity: 1.0
```

#### Main Light (Основной)
```typescript
position: [mainLightX, 50, mainLightZ]
intensity: 1.5
color: '#ffffff'
castShadow: true
shadow-mapSize: [2048, 2048]
shadow-radius: 4  // Очень мягкие тени
```

#### Additional Directional Lights (2 шт)
```typescript
// Слева
position: [-30, 40, -30]
intensity: 0.8
color: '#f5f5ff'  // Холодный оттенок

// Справа
position: [30, 40, 30]
intensity: 0.8
color: '#fff5f5'  // Теплый оттенок
```

#### Point Lights (6 шт)
```typescript
// Центральный сверху
position: [0, 35, 0]
intensity: 1.5
distance: 100

// По углам (4 шт)
positions: [±40, 20, ±40]
intensity: 1.0
colors: ['#e8f4ff', '#ffe8f4', '#f4ffe8', '#fff4e8']  // Разные оттенки
distance: 80
decay: 2
```

#### Bottom Light (Нижний)
```typescript
position: [0, -10, 0]
intensity: 0.4
color: '#d8d8d8'
castShadow: false
```

### 📊 Производительность
- **Shadow maps**: 2048x2048 (средняя нагрузка)
- **Количество источников**: 11 (много!)
- **FPS impact**: Средний-высокий

### 🎨 Визуальный результат
- Максимальная яркость
- Минимальные тени
- Равномерное освещение
- "Чистый" вид
- Отсутствие темных областей

### 💡 Советы по настройке
```typescript
// Еще ярче
ambientLight.intensity = 1.5
hemisphereLight.intensity = 1.2

// Убрать тени совсем
mainLight.castShadow = false

// Добавить цветные акценты
pointLights.forEach((light, i) => {
  light.color = vibrantColors[i]
})
```

---

## Вариант 4: Custom Lighting (Оригинальное освещение)

### 🔧 Описание
Оригинальное освещение из проекта. Простая схема с одним направленным светом.

### ✨ Характеристики
- **Hemisphere Light** - базовое освещение
- **Directional Light** - один основной источник
- Привязка к вращению HDRI

### 🎯 Когда использовать
- Для возврата к оригинальному виду
- Минимальная нагрузка
- Простая схема

### 🔧 Технические параметры

#### Hemisphere Light
```typescript
color: '#ffffff'
groundColor: '#444444'
intensity: 1.5
```

#### Directional Light
```typescript
position: [lightX, 20, lightZ]  // Привязано к HDRI rotation
intensity: 3.0
castShadow: true
shadow-mapSize: [1024, 1024]
shadow-radius: 2
```

### 📊 Производительность
- **Shadow maps**: 1024x1024 (низкая нагрузка)
- **Количество источников**: 2
- **FPS impact**: Минимальный

---

## Сравнительная таблица

| Характеристика | Studio | Architectural | Exhibition | Custom |
|---------------|--------|---------------|------------|--------|
| **Реализм** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| **Яркость** | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Детализация** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **Производительность** | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Количество источников** | 6-8 | 4-8 | 11 | 2 |
| **Shadow Resolution** | 2048 | 4096 | 2048 | 1024 |
| **Время суток** | ✅ | ✅ | ❌ | ❌ |
| **Ночное освещение** | ✅ | ✅ | ❌ | ❌ |

---

## Быстрый старт

### 1. Попробовать все варианты

Откройте консоль браузера (F12) и выполните:

```javascript
// Настройка функции переключения
const store = window.__ZUSTAND_STORE__ || (() => {
  const iframe = document.querySelector('iframe')
  return iframe?.contentWindow?.__ZUSTAND_STORE__
})()

if (store) {
  window.switchLighting = (preset) => {
    store.setState({ lightingPreset: preset })
    console.log(`✅ Switched to ${preset} lighting`)
  }
  
  console.log('Available presets:')
  console.log('- switchLighting("studio")')
  console.log('- switchLighting("architectural")')
  console.log('- switchLighting("exhibition")')
  console.log('- switchLighting("custom")')
}
```

### 2. Изменить время суток (для architectural)

```javascript
// Установить время
const setTime = (hour) => {
  store.setState({ timeOfDay: hour })
  console.log(`⏰ Time set to ${hour}:00`)
}

setTime(6)   // Рассвет
setTime(12)  // Полдень
setTime(18)  // Закат
setTime(22)  // Ночь
```

### 3. Настроить HDRI вращение

```javascript
// Повернуть окружение
const rotateHDRI = (degrees) => {
  store.setState({ hdriRotation: degrees })
  console.log(`🔄 HDRI rotated to ${degrees}°`)
}

rotateHDRI(0)    // Север
rotateHDRI(90)   // Восток
rotateHDRI(180)  // Юг
rotateHDRI(270)  // Запад
```

---

## Рекомендации

### Для презентаций
✅ **Studio Lighting** - лучший баланс

### Для архитектурной визуализации
✅ **Architectural Lighting** - максимальный реализм

### Для веселых/игровых проектов
✅ **Exhibition Lighting** - яркий и чистый

### Для производительности
✅ **Custom Lighting** - минимальная нагрузка

---

## Дальнейшие улучшения

### Возможные добавления:
1. **Toon Lighting** - для cartoon стиля
2. **Dramatic Lighting** - для кинематографического вида
3. **Sunset Lighting** - специальный пресет для заката
4. **Indoor Lighting** - для интерьеров

### Настраиваемые параметры:
- Интенсивность каждого источника
- Цвета источников
- Размер и мягкость теней
- Количество источников

---

*Документ создан: 2026-02-13*
*Версия: 1.0*
